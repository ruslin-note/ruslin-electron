import { Button } from "@/components";
import "./Notes.scss";

export const Notes = () => {
  return (
    <div class="notes-container">
      <Button variant="accent">New note</Button>
    </div>
  );
};
